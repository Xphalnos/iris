name: Release CI

env:
  LLVM_VER: 22

on:
  push:
    tags:
    - '0.*'

jobs:
  windows-build:
    runs-on: windows-2025-vs2026

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: 'recursive'
        fetch-depth: 0

    - name: Install LLVM
      shell: pwsh
      run: |
        choco install llvm --version=${{env.LLVM_VER}}.1.0 -y --force --no-progress

    - name: Configure build for amd64
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: amd64

    - name: Build and pack Iris
      run: |
        git fetch --all --tags
        cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang-cl -DCMAKE_CXX_COMPILER=clang-cl
        cmake --build build
        New-Item -Path "dist" -ItemType Directory
        Copy-Item -Path ".\build\iris.exe" "dist"

    - name: Generate artifact name
      id: generate-name
      run: |
        echo "::set-output name=artifact::iris-${{ github.ref_name }}-windows"

    - name: Zip artifact
      run: |
        $artifactName = "${{ steps.generate-name.outputs.artifact }}"
        Compress-Archive -Path "dist\*" -DestinationPath "$artifactName.zip"

    - name: Upload to release
      uses: softprops/action-gh-release@v3
      with:
        files: |
          ${{ steps.generate-name.outputs.artifact }}.zip

  linux-build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: 'recursive'
        fetch-depth: 0

    - name: Install deps
      run: |
        sudo add-apt-repository universe
        sudo apt update
        sudo apt-get install git make \
        pkg-config cmake ninja-build gnome-desktop-testing libasound2-dev libpulse-dev \
        libaudio-dev libjack-dev libsndio-dev libx11-dev libxext-dev \
        libxrandr-dev libxcursor-dev libxfixes-dev libxi-dev libxss-dev libxtst-dev \
        libxkbcommon-dev libdrm-dev libgbm-dev libgl1-mesa-dev libgles2-mesa-dev \
        libegl1-mesa-dev libdbus-1-dev libibus-1.0-dev libudev-dev libfuse2t64

    - name: Install LLVM
      run: |
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh ${{env.LLVM_VER}}

    - name: Build and pack Iris
      run: |
        git fetch --all --tags
        cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang-${{env.LLVM_VER}} -DCMAKE_CXX_COMPILER=clang++-${{env.LLVM_VER}}
        cmake --build build
        sudo cmake --install build
        mkdir dist
        mv ./build/iris ./dist
        mkdir appimage
        mv ./build/*.AppImage ./appimage

    - name: Generate artifact name
      id: generate-name
      run: |
        echo "::set-output name=artifact::iris-${{ github.ref_name }}-linux"

    - name: Generate AppImage artifact name
      id: generate-name-appimage
      run: |
        echo "::set-output name=artifact::iris-${{ github.ref_name }}-linux-appimage"

    - name: Zip artifact
      run: |
        zip -rj ${{ steps.generate-name.outputs.artifact }}.zip dist

    - name: Zip AppImage
      run: |
        zip -rj ${{ steps.generate-name-appimage.outputs.artifact }}.zip appimage

    - name: Upload normal binary to release
      uses: softprops/action-gh-release@v3
      with:
        files: |
          ${{ steps.generate-name.outputs.artifact }}.zip

    - name: Upload AppImage to release
      uses: softprops/action-gh-release@v3
      with:
        files: |
          ${{ steps.generate-name-appimage.outputs.artifact }}.zip

  macos-universal-build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: 'recursive'
        fetch-depth: 0

    - name: Install Vulkan SDK
      uses: jakoch/install-vulkan-sdk-action@v1
      with:
        optional_components: com.lunarg.vulkan.volk
        install_runtime: true
        cache: true
        stripdown: true

    - name: Install LLVM
      run: |
          brew install llvm@${{env.LLVM_VER}}

    - name: Build and pack Iris
      run: |
        git fetch --all --tags
        export PATH="/opt/homebrew/opt/llvm@${{env.LLVM_VER}}/bin:$PATH"
        export CPPFLAGS="-I/opt/homebrew/opt/llvm@${{env.LLVM_VER}}/include"
        export LDFLAGS="-L/opt/homebrew/opt/llvm@${{env.LLVM_VER}}/lib"
        cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=$(brew --prefix llvm@${{env.LLVM_VER}})/bin/clang -DCMAKE_CXX_COMPILER=$(brew --prefix llvm@${{env.LLVM_VER}})/bin/clang++
        cmake --build build
        sudo cmake --install build

    - name: Generate artifact name
      id: generate-name
      run: |
        echo "::set-output name=artifact::iris-${{ github.ref_name }}-macos-universal"

    - name: Zip artifact
      run: |
        cd build; zip -r ../${{ steps.generate-name.outputs.artifact }}.zip ./iris.app; cd ..

    - name: Upload to release
      uses: softprops/action-gh-release@v3
      with:
        files: |
          ${{ steps.generate-name.outputs.artifact }}.zip
